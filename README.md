# `bridge-dds`

`bridge-dds` is a `DDS`-backed bridge analysis tool. Given a `PBN` record with `Deal`, `Dealer`, and `Vulnerable` tags, it computes the `4x5` double-dummy tricks matrix, derives the `DealerPar` result, and later will analyze played-card traces.
